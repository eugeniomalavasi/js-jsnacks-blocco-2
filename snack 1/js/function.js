/**
 * contare numero elementi presenti in un array
 * @param {array}
 * @returns {number}
 */
function arrayCounter(array) {
    let arrayLenght;
    for (i = 0; i < array.length; i++) {
        arrayLenght = array[i];
    }
    console.log("gli elementi in questo array sono:", i);
    return arrayLenght;
}
